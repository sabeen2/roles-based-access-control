import { apiList } from "..";

const { userLogin } = apiList.userAuth;

// export const initSaveBasicDetails = async (requestData: ILoginRequest) => {
//   try {
//     const data = await makeHttpRequest(userLogin, {
//       requestData,
//     });

//     return data;
//   } catch (error) {
//     throw error;
//   }
// };
